<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.codenova.moneylog.repository.ExpensesRepository">
    <insert id="save">
        insert into expenses(user_id, expense_date, description, amount, category_id) values (#
    </insert>
    <select id="findByUserId" resultType="">
        select * from expenses where user_id=#{userId} order by expense_date asc, id asc
    </select>
    <select id="findByUserIdAndDuration" resultType="">
        (@Param("userId") String userId, @Param("startDate") LocalDate startDate, @Param("endDate") LocalDate endDate)
        select * from expenses where user_id=#{userId} and expense_date between #{startDate} and #{endDate}  order by expense_date asc, id asc
    </select>
</mapper>